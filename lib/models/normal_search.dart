enum SearchIn { all, airport, external_branch }

enum ShowType { map, list }

enum WindowType { all, normal, automatic }

enum LimeType { all, normal, automatic }

enum OutsideOfKingdom { all, normal, automatic }
